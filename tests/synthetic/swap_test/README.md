# Swap Test

This directory contains synthetic reads for swap square optimisation testing.

- [Swap Square Test Design](data/README.md)

Use the CLI with the provided FASTQ files and `--optimise-diagonal` to validate swap square detection and maximal diagonal sum assembly.